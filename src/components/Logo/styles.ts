import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        paddingLeft: 20,
        paddingVertical: 30,
    },
    logo: {
        color: 'white',
        fontWeight: 'bold',
        fontSize: 24,
    }
});